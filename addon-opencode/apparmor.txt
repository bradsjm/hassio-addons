#include <tunables/global>

profile opencode flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/bash>

  network inet stream,
  network inet6 stream,
  network inet dgram,
  network inet6 dgram,
  network netlink raw,
  network unix stream,

  capability net_bind_service,
  capability dac_override,
  capability dac_read_search,
  capability chown,
  capability setgid,
  capability setuid,

  /bin/** ix,
  /usr/bin/** ix,
  /usr/local/bin/** ix,
  /etc/s6/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,

  /data/opencode/.opencode/bin/opencode ix,
  /data/opencode/npm-global/bin/** ix,

  /tmp/** ix,

  /config/** rw,
  /config/opencode/** rw,
  /data/** rw,
  /share/** rw,
  /tmp/** rw,
  /run/** rw,

  / r,
  /** r,

  /lib/** mr,
  /usr/lib/** mr,
  /usr/local/lib/** mr,

  deny /proc/** rwl,
  deny /sys/** rwl,

  /dev/null rw,
  /dev/random r,
  /dev/urandom r,
  /dev/zero rw,
  /dev/tty rw,
  /dev/pts/** rw,
  /dev/console rw,

  /run/s6-linux-init-container-results/exitcode rw,
}
